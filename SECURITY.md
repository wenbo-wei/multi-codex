# Security policy

Security fixes are provided for the latest `main` revision.

Report vulnerabilities through GitHub private vulnerability reporting for
`wenbo-wei/multi-codex`. Do not attach terminal contents, Codex sessions, user
settings dumps, or other private desktop data.

The installer changes only the current user's GNOME extension files and
extension settings. It never needs root access.
